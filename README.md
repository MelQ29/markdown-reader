# MD Reader

Простое веб-приложение для загрузки и чтения Markdown файлов.

## Возможности

- 📁 Загрузка .md файлов через веб-интерфейс
- 📖 Удобное чтение Markdown файлов с подсветкой синтаксиса
- 📋 Список всех загруженных файлов в боковой панели
- 🗑️ Удаление файлов

## Установка

### Linux и macOS

#### Автоматическая установка (рекомендуется)

Запустите скрипт установки:
```bash
chmod +x setup.sh
./setup.sh
```

#### Ручная установка

1. Создайте виртуальное окружение:
```bash
python3 -m venv venv
```

2. Активируйте виртуальное окружение:
```bash
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

### Windows

#### Автоматическая установка (рекомендуется)

Запустите скрипт установки:
```cmd
setup.bat
```

#### Ручная установка

1. Создайте виртуальное окружение:
```cmd
python -m venv venv
```

2. Активируйте виртуальное окружение:
```cmd
venv\Scripts\activate
```

3. Установите зависимости:
```cmd
pip install -r requirements.txt
```

## Запуск

### Linux и macOS

#### Автоматический запуск

```bash
chmod +x run.sh
./run.sh
```

#### Ручной запуск

1. Активируйте виртуальное окружение (если еще не активировано):
```bash
source venv/bin/activate
```

2. Запустите приложение:
```bash
python app.py
```

### Windows

#### Автоматический запуск

```cmd
run.bat
```

#### Ручной запуск

1. Активируйте виртуальное окружение (если еще не активировано):
```cmd
venv\Scripts\activate
```

2. Запустите приложение:
```cmd
python app.py
```

После запуска откройте браузер и перейдите по адресу: http://127.0.0.1:5000

## Использование

1. Нажмите кнопку "Загрузить .md файл" в боковой панели
2. Выберите файл .md на вашем компьютере
3. Файл автоматически откроется для просмотра
4. Кликните на любой файл в списке слева, чтобы открыть его
5. Используйте кнопку "✕" для удаления файла

## Структура проекта

```
md-reader/
├── app.py              # Основное Flask приложение
├── templates/
│   └── index.html      # HTML шаблон
├── uploads/            # Папка для загруженных файлов (создается автоматически)
├── venv/               # Виртуальное окружение Python (создается при установке)
├── requirements.txt    # Зависимости Python
├── setup.sh           # Скрипт автоматической установки (Linux/macOS)
├── run.sh             # Скрипт автоматического запуска (Linux/macOS)
├── setup.bat          # Скрипт автоматической установки (Windows)
├── run.bat            # Скрипт автоматического запуска (Windows)
└── README.md          # Этот файл
```

