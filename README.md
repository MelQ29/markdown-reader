# MD Reader

Простое и удобное веб-приложение для чтения и редактирования Markdown файлов прямо в браузере.

![markdown-reader-demo](https://github.com/user-attachments/assets/a26b705b-f0ff-409f-b2f6-670425a99e52)

## Возможности

### Управление файлами
- 📁 **Загрузка .md файлов**: Легко загружайте файлы через веб-интерфейс.
- ✏️ **Переименование**: Редактируйте названия файлов прямо в списке.
- 🗑️ **Удаление**: Удаляйте ненужные файлы.
- ⚠️ **Обработка дубликатов**: При загрузке файла с уже существующим именем приложение предложит вам переименовать его.

### Чтение и Редактирование
- 📖 **Удобное чтение**: Наслаждайтесь чтением Markdown с красивым форматированием и стилизацией.
- ✨ **Подсветка синтаксиса**: Блоки кода автоматически подсвечиваются для лучшей читаемости.
- 🔄 **Переключение режимов**: Мгновенно переключайтесь между режимом просмотра и режимом редактирования.
- ✍️ **Встроенный редактор**: Редактируйте содержимое ваших .md файлов прямо в приложении.
- 🛠️ **Панель инструментов Markdown**: Используйте удобную панель для быстрого применения стилей: заголовков, жирного текста, курсива, списков, цитат и многого другого.
- 🪟 **Сплит-редактор и предпросмотр**: Режим редактирования делит экран 50/50 — слева сырой Markdown, справа живой предпросмотр с подсветкой.
- ✅ **Сохранение через diff-предпросмотр**: Перед записью изменений показывается модальное окно сравнения «до/после» (сырой diff-таблица и отрендеренный вид), можно подтвердить или отменить.
- ↩️ **Отмена без записи**: В режиме редактирования доступна кнопка «Отменить», которая возвращает содержимое к исходному состоянию без сохранения.

## Установка

### Linux и macOS

#### Автоматическая установка (рекомендуется)

Запустите скрипт установки:
```bash
chmod +x setup.sh
./setup.sh
```

#### Ручная установка

1. Создайте виртуальное окружение:
```bash
python3 -m venv venv
```

2. Активируйте виртуальное окружение:
```bash
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

### Windows

#### Автоматическая установка (рекомендуется)

Запустите скрипт установки:
```cmd
setup.bat
```

#### Ручная установка

1. Создайте виртуальное окружение:
```cmd
python -m venv venv
```

2. Активируйте виртуальное окружение:
```cmd
venv\Scripts\activate
```

3. Установите зависимости:
```cmd
pip install -r requirements.txt
```

## Запуск

### Linux и macOS

#### Автоматический запуск

```bash
chmod +x run.sh
./run.sh
```

#### Ручной запуск

1. Активируйте виртуальное окружение (если еще не активировано):
```bash
source venv/bin/activate
```

2. Запустите приложение:
```bash
python app.py
```

### Windows

#### Автоматический запуск

```cmd
run.bat
```

#### Ручной запуск

1. Активируйте виртуальное окружение (если еще не активировано):
```cmd
venv\Scripts\activate
```

2. Запустите приложение:
```cmd
python app.py
```

После запуска откройте браузер и перейдите по адресу: http://127.0.0.1:5000

## Работа с локальными файлами (без копий)

После запуска откройте браузер и выберите, с чем работать, через кнопки в интерфейсе.

### Требования браузера

- Для выбора локальной папки/файлов используется File System Access API — рекомендуется Chrome/Edge 86+ или другой Chromium-браузер с поддержкой API.
- Windows может блокировать системные папки (например, Downloads); выберите обычную папку или конкретные файлы.

## Использование

1. **Откройте папку или файлы** через кнопки в боковой панели:
   - `📂 Открыть папку` — выбрать директорию, все `.md` файлы из неё попадут в список
   - `📁 Открыть файлы` — выбрать один или несколько файлов (при выборе папки сама папка может не показывать файлы в диалоге, но после выбора список .md появится в приложении)
   - `➕ Добавить файлы` — добавить новые файлы к уже открытому списку
   - `🗑 Убрать все` — очистить список выбранных файлов (файлы на диске не удаляются)
2. **Выберите файл** из списка, чтобы просмотреть или отредактировать его.
3. **Удаление из списка** (иконка ✕) убирает файл только из редактора, не удаляя его с диска.
4. **Для редактирования содержимого** открытого файла нажмите кнопку "Редактировать".
5. В режиме редактирования используйте **панель инструментов** для быстрого форматирования текста.
6. Нажмите **"Сохранить"**, чтобы сохранить изменения в исходный файл (без копий).
7. Перед записью откроется diff: сравните изменения и подтвердите сохранение либо отмените.
8. В режиме редактирования кнопка «Отменить» откатывает содержимое к исходному состоянию без записи файла.

## Структура проекта

```
md-reader/
├── app.py              # Точка входа (app factory + регистрация blueprints)
├── config.py           # Конфигурация Flask и ограничений загрузки
├── routes/
│   └── api.py          # API endpoints, сгруппированные в blueprint
├── static/             # Вынесенные ассеты (CSS/JS)
│   ├── css/main.css
│   └── js/main.js
├── services/           # Бизнес-логика и утилиты
│   ├── diff_service.py
│   ├── file_service.py
│   └── markdown_service.py
├── templates/
│   └── index.html      # HTML шаблон
├── uploads/            # Папка для загруженных файлов (используется по умолчанию, если WORK_DIR не указан; при работе через UI с локальными файлами не используется)
├── requirements.txt    # Зависимости Python
├── setup.sh            # Скрипт автоматической установки (Linux/macOS)
├── run.sh              # Скрипт автоматического запуска (Linux/macOS)
├── setup.bat           # Скрипт автоматической установки (Windows)
├── run.bat             # Скрипт автоматического запуска (Windows)
└── README.md           # Этот файл
```
