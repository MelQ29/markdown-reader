# MD Reader

Простое и удобное веб-приложение для чтения и редактирования Markdown файлов прямо в браузере.

![markdown-reader-demo](https://github.com/user-attachments/assets/a26b705b-f0ff-409f-b2f6-670425a99e52)

## Возможности

### Управление файлами
- 📁 **Загрузка .md файлов**: Легко загружайте файлы через веб-интерфейс.
- ✏️ **Переименование**: Редактируйте названия файлов прямо в списке.
- 🗑️ **Удаление**: Удаляйте ненужные файлы.
- ⚠️ **Обработка дубликатов**: При загрузке файла с уже существующим именем приложение предложит вам переименовать его.

### Чтение и Редактирование
- 📖 **Удобное чтение**: Наслаждайтесь чтением Markdown с красивым форматированием и стилизацией.
- ✨ **Подсветка синтаксиса**: Блоки кода автоматически подсвечиваются для лучшей читаемости.
- 🔄 **Переключение режимов**: Мгновенно переключайтесь между режимом просмотра и режимом редактирования.
- ✍️ **Встроенный редактор**: Редактируйте содержимое ваших .md файлов прямо в приложении.
- 🛠️ **Панель инструментов Markdown**: Используйте удобную панель для быстрого применения стилей: заголовков, жирного текста, курсива, списков, цитат и многого другого.
- 🪟 **Сплит-редактор и предпросмотр**: Режим редактирования делит экран 50/50 — слева сырой Markdown, справа живой предпросмотр с подсветкой.
- ✅ **Сохранение через diff-предпросмотр**: Перед записью изменений показывается модальное окно сравнения «до/после» (сырой diff-таблица и отрендеренный вид), можно подтвердить или отменить.
- ↩️ **Отмена без записи**: В режиме редактирования доступна кнопка «Отменить», которая возвращает содержимое к исходному состоянию без сохранения.

## Установка

### Linux и macOS

#### Автоматическая установка (рекомендуется)

Запустите скрипт установки:
```bash
chmod +x setup.sh
./setup.sh
```

#### Ручная установка

1. Создайте виртуальное окружение:
```bash
python3 -m venv venv
```

2. Активируйте виртуальное окружение:
```bash
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

### Windows

#### Автоматическая установка (рекомендуется)

Запустите скрипт установки:
```cmd
setup.bat
```

#### Ручная установка

1. Создайте виртуальное окружение:
```cmd
python -m venv venv
```

2. Активируйте виртуальное окружение:
```cmd
venv\Scripts\activate
```

3. Установите зависимости:
```cmd
pip install -r requirements.txt
```

## Запуск

### Linux и macOS

#### Автоматический запуск

```bash
chmod +x run.sh
./run.sh
```

#### Ручной запуск

1. Активируйте виртуальное окружение (если еще не активировано):
```bash
source venv/bin/activate
```

2. Запустите приложение:
```bash
python app.py
```

### Windows

#### Автоматический запуск

```cmd
run.bat
```

#### Ручной запуск

1. Активируйте виртуальное окружение (если еще не активировано):
```cmd
venv\Scripts\activate
```

2. Запустите приложение:
```cmd
python app.py
```

После запуска откройте браузер и перейдите по адресу: http://127.0.0.1:5000

## Использование

1. **Загрузите файл**, используя кнопку в боковой панели.
2. **Выберите файл** из списка, чтобы просмотреть его.
3. **Для переименования** файла нажмите на иконку карандаша (✏️).
4. **Для удаления** файла нажмите на иконку крестика (✕).
5. **Для редактирования содержимого** открытого файла нажмите кнопку "Редактировать".
6. В режиме редактирования используйте **панель инструментов** для быстрого форматирования текста.
7. Нажмите **"Сохранить"**, чтобы сохранить изменения.
8. При сохранении откроется модальное окно diff: сравните изменения в сыром виде или в отрендеренном markdown и подтвердите «Сохранить» либо нажмите «Отменить», чтобы вернуться к редактированию без записи.
9. В режиме редактирования есть отдельная кнопка «Отменить», которая откатывает содержимое к исходному состоянию (до начала правок) без сохранения файла.

## Структура проекта

```
md-reader/
├── app.py              # Основное Flask приложение
├── templates/
│   └── index.html      # HTML шаблон
├── uploads/            # Папка для загруженных файлов (создается автоматически)
├── venv/               # Виртуальное окружение Python (создается при установке)
├── requirements.txt    # Зависимости Python
├── setup.sh           # Скрипт автоматической установки (Linux/macOS)
├── run.sh             # Скрипт автоматического запуска (Linux/macOS)
├── setup.bat          # Скрипт автоматической установки (Windows)
├── run.bat            # Скрипт автоматического запуска (Windows)
└── README.md          # Этот файл
```
